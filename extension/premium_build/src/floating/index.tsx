import React from 'react';
import ReactDOM from 'react-dom/client';
import FloatingWindow from '../popup/FloatingWindow';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FloatingWindow />
  </React.StrictMode>
); 